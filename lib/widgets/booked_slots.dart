import 'package:flutter/material.dart';






class BookedSlots extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListTile(
      title: Text('s'), 
    );
  }
}
